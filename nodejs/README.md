1. Source code
.js

2. Package manager
❌ None required (can run directly in browser)
Optional (for modern projects):
npm / yarn (used for tooling, not required by the language itself)

3. Dependency file
❌ None (basic usage)
Optional:
package.json (only if using build tools like React, Vue, etc.)

4. Runtime / Package / Executable
Runtime: Web Browser (e.g. Chrome, Firefox)
Package:
Plain .js files
or bundled JS (via webpack, Vite, etc.)
Executable:
Runs via <script> tag in HTML
