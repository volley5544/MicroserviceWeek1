A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.

1. Source code
.dart

2. Package manager
pub

3. Dependency file
pubspec.yaml

4. Runtime / Package / Executable
Runtime: Dart VM / Flutter Engine
Package:
Dart package
Flutter app bundle (APK, IPA, Web)
Executable:
dart run
Flutter → mobile/web apps
